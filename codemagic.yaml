workflows:
  ios-release:
    name: iOS Release
    max_build_duration: 30
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Personal
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.amailogy.nanmonme
      vars:
        XCODE_WORKSPACE: "ios/App/App.xcodeproj"
        TEAM_ID: "9ZQY7HY294"
      node: 20
      xcode: latest
    scripts:
      - name: Install Node dependencies
        script: |
          npm ci
      - name: Build web app
        script: |
          npm run build
      - name: Sync Capacitor
        script: |
          npx cap sync ios
      - name: Update signing settings
        script: |
          cd ios/App
          xcode-project use-profiles
          xcodebuild -project App.xcodeproj \
            -scheme App \
            -configuration Release \
            DEVELOPMENT_TEAM="$TEAM_ID" \
            CODE_SIGN_STYLE="Manual" \
            -showBuildSettings | grep -E "DEVELOPMENT_TEAM|CODE_SIGN|PROVISIONING" || true
      - name: Build iOS app
        script: |
          cd ios/App
          xcodebuild -project App.xcodeproj \
            -scheme App \
            -sdk iphoneos \
            -configuration Release \
            -archivePath build/App.xcarchive \
            DEVELOPMENT_TEAM="$TEAM_ID" \
            archive
      - name: Export IPA
        script: |
          cd ios/App
          xcodebuild -exportArchive \
            -archivePath build/App.xcarchive \
            -exportOptionsPlist /dev/stdin \
            -exportPath build/export \
            <<< '<?xml version="1.0" encoding="UTF-8"?>
            <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
            <plist version="1.0"><dict>
              <key>method</key><string>app-store</string>
              <key>teamID</key><string>9ZQY7HY294</string>
              <key>uploadBitcode</key><false/>
              <key>uploadSymbols</key><true/>
            </dict></plist>'
    artifacts:
      - ios/App/build/export/*.ipa
